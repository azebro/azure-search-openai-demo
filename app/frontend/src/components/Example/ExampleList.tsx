import { Example } from "./Example";

import styles from "./Example.module.css";

export type ExampleModel = {
    text: string;
    value: string;
};

const EXAMPLES: ExampleModel[] = [
    {
        text: "What are the contractual conditions for IT maintenance?",
        value: "What are the contractual conditions for IT maintenance?"
    },
    { text: "What are conditions for cartel prevention?", value: "What are conditions for cartel prevention?" },
    { text: "what are the terms of quality in procurement?", value: "what are the terms of quality in procurement?" }
];

interface Props {
    onExampleClicked: (value: string) => void;
}

export const ExampleList = ({ onExampleClicked }: Props) => {
    return (
        <ul className={styles.examplesNavList}>
            {EXAMPLES.map((x, i) => (
                <li key={i}>
                    <Example text={x.text} value={x.value} onClick={onExampleClicked} />
                </li>
            ))}
        </ul>
    );
};
